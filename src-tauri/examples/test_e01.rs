// Test E01 extraction
// Since containers module is private, we'll test through direct e01 module calls
extern crate liblfx_lib;

fn main() {
    // Just run cargo test instead - the e01 module has internal functions we can't access
    println!("Use the Tauri app or run 'npm run tauri dev' to test E01 functionality");
    println!("Or check the test files in e01.rs");
}
